exports.handler = async (event) => {
  // TODO: verify webhook signature with Square
  console.log("Webhook received:", event.body);

  return {
    statusCode: 200,
    body: JSON.stringify({ success: true }),
  };
};